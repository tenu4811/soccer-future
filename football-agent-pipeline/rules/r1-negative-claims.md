# R1 否定性声称总规则

任何"无/未/0/从不/尚未"类声称，**必须挂数据源标签**（[FBref]/[TM]/[WhoScored]），否则视为未验证。

## 违规示例
```
❌ 两队无历史交锋记录。（为什么？你查了哪个数据源？）
✅ 两队无历史交锋记录 [FBref H2H]。（查了，确实没踢过）
```

## 执行
阶段出口门禁 grep `无|未|[0-9]次交手|[0-9]次交锋|从未|尚未`，每条命中必须挂源标签。
