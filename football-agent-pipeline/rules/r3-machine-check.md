# R3 自评机器化

最终评分前强制跑机器校验，**人感自评一律作废**。自评须附机器输出数值。

## 强制执行的检查
- `wc -m` 字符数 → D8
- grep 拉丁字母姓名 → D10
- grep 源标签种类 → D9
- grep 估算标记（估/~/约/大约/待补充）→ D1/D6
- grep YAML 数据块（stage:）→ D11

## 违规
```
❌ 自评：D8=87,000字（估计）
✅ 自评：D8=87,421字（wc -m输出）
```
